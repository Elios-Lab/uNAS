scikit-learn
ray
dragonfly-opt
matplotlib
datasets                 # HuggingFace datasets (mini-imagenet and other HF datasets)
tensorflow-model-optimization
setuptools<60.0.0

#windows related dependencies
tensorflow<2.11; sys_platform == 'win32'
numpy==1.23.5; sys_platform == 'win32'

#linux related dependencies
tensorflow[and-cuda]; sys_platform == 'linux'
numpy; sys_platform == 'linux'

#macOS related dependencies (Apple Silicon MPS acceleration)
tensorflow; sys_platform == 'darwin'
tensorflow-metal; sys_platform == 'darwin'
numpy; sys_platform == 'darwin'
